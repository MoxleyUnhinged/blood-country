CREATE TABLE IF NOT EXISTS notes (
  section TEXT PRIMARY KEY,
  note TEXT
);

INSERT INTO notes (section, note) VALUES
('character', 'Field observations:\n- Tracks vanish at dry creek bend\n- Min Min glow seen near ridge after midnight');
