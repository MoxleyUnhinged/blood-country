CREATE TABLE IF NOT EXISTS notes (
  section TEXT PRIMARY KEY,
  note TEXT
);

INSERT INTO notes (section, note) VALUES
('gm', 'Scene beats:\n- Escalate omen after first failed fright check\n- Reveal old station ledger clue in scene 2');
